package aTableWithGlasses.model;

public interface Glassable {

    /**
     * Method for pour from glass;
     * If glass is not Empty will pour from it, and will print what type of glass;
     * variable boolean isEmpty will change;
     *
     * if glass is empty, will print about it and don`t change variable IsEmpty;
     */
    void pourFromMe();
}
